diff --git a/kernel/sched/cpufreq_schedutil.c b/kernel/sched/cpufreq_schedutil.c
index 1234567..abcdefg 100644
--- a/kernel/sched/cpufreq_schedutil.c
+++ b/kernel/sched/cpufreq_schedutil.c
@@ -50,8 +50,8 @@ struct sugov_cpu {
 	unsigned int		iowait_boost;
 	unsigned int		iowait_boost_max;
 	u64			last_update;
-	unsigned long		util;
-	unsigned long		bw_dl;
+	unsigned long		util_avg;
+	unsigned long		bw_dl_avg;
 	unsigned long		max;
 
 	/* The field below is for single-freq setups only */
@@ -155,6 +155,12 @@ static unsigned int get_next_freq(struct sugov_policy *sg_policy,
 	freq = (freq + (freq >> 2)) * util / max;
 
+	/*
+	 * PocoOptimize: Aggressive upscaling for better responsiveness
+	 * Boost frequency by 15% during interactive workloads
+	 */
+	if (sg_policy->tunables->boost_enabled && util > (max >> 1))
+		freq += (freq * 15) / 100;
+
 	if (unlikely(freq == 0)) {
 		if (util_max > sg_policy->saved_util_max) {
 			sg_policy->saved_util_max = util_max;
@@ -200,7 +206,7 @@ static void sugov_get_util(struct sugov_cpu *sg_cpu)
 {
 	struct rq *rq = cpu_rq(sg_cpu->cpu);
 	unsigned long util = cpu_util_cfs(rq);
-	unsigned long max = arch_scale_cpu_capacity(NULL, sg_cpu->cpu);
+	unsigned long max = arch_scale_cpu_capacity(sg_cpu->cpu);
 
 	sg_cpu->max = max;
 	sg_cpu->bw_dl = cpu_bw_dl(rq);
@@ -210,6 +216,13 @@ static void sugov_get_util(struct sugov_cpu *sg_cpu)
 	 */
 	sg_cpu->util = schedutil_cpu_util(sg_cpu->cpu, util, max,
 					  FREQUENCY_UTIL, NULL);
+
+	/*
+	 * PocoOptimize: Reduce latency for interactive tasks
+	 * Apply faster ramp-up when utilization increases rapidly
+	 */
+	if (sg_cpu->util > sg_cpu->util_avg + (max >> 3))
+		sg_cpu->util = min(sg_cpu->util + (max >> 4), max);
 }
 
 /**
@@ -450,7 +463,7 @@ static int sugov_init(struct cpufreq_policy *policy)
 		tunables->rate_limit_us = cpufreq_policy_transition_delay_us(policy);
 	}
 
-	tunables->hispeed_freq = policy->max;
+	tunables->hispeed_freq = policy->max * 90 / 100;  /* PocoOptimize: 90% for efficiency */
 	tunables->hispeed_load = DEFAULT_HISPEED_LOAD;
 	tunables->boost = 0;
 
diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index 2345678..bcdefgh 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -4500,6 +4500,14 @@ place_entity(struct cfs_rq *cfs_rq, struct sched_entity *se, int initial)
 	u64 vruntime = cfs_rq->min_vruntime;
 
 	/*
+	 * PocoOptimize: Disable gentle fair sleepers for gaming
+	 * This improves frame pacing and reduces input latency
+	 * Trade-off: Slightly less fair scheduling
+	 */
+#ifdef CONFIG_POCOOPTIMIZE_GAMING
+	goto skip_sleeper_bonus;
+#endif
+
+	/*
 	 * The 'current' period is already promised to the current tasks,
 	 * however the extra weight of the new task will slow them down a
 	 * little, place the new task so that it fits in the slot that
@@ -4517,6 +4525,7 @@ place_entity(struct cfs_rq *cfs_rq, struct sched_entity *se, int initial)
 		vruntime -= thresh;
 	}
 
+skip_sleeper_bonus:
 	/* ensure we never gain time by being placed backwards. */
 	se->vruntime = max_vruntime(se->vruntime, vruntime);
 }
